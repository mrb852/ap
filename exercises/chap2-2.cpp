#include <iostream>
// Command to compile: g++ chap2-2.cpp chap2-2
int main(int argc, char const *argv[]) {
  double p, q, x, y;
  int j;
  if (p >= q || j != 10) {
    x = 5.0;
  } else if (y >= q && j == 20) {
    x = 5.0;
  }
  return 0;
}
